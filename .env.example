# CALL-E credentials (get a key at https://dashboard.heycall-e.com/account/api-keys)
CALLE_API_KEY=
CALLE_BASE_URL=https://api.heycall-e.com
# Optional: Supabase/Postgres for durable storage (app runs with local JSON store if unset)
DATABASE_URL=
# Optional: webhook for async CALL-E completion events
CALLE_WEBHOOK_URL=
